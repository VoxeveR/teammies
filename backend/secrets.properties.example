# Copy to secrets.properties (do not commit) and fill with real values

db.username=CHANGE_ME

db.password=CHANGE_ME

# Should be a long, random secret. Prefer base64 for compatibility with JwtService.
# Example generation (Linux/macOS): openssl rand -base64 48
jwt.secretKey=CHANGE_ME
